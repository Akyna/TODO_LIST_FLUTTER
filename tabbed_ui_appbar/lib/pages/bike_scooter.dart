import 'package:flutter/material.dart';

class BikeScooter extends StatelessWidget {
  const BikeScooter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.indigo,
      body: Center(
        child: Text("BikeScooter"),
      ),
    );
  }
}
